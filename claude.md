# Anthropo-Reader 开发指南

## 📋 项目概述

Anthropo-Reader 是一款专为工程背景用户设计的自动化信息聚合 App，通过 Flutter 构建，具备双主题视觉系统（羊皮纸模式 + 极夜模式）。
旨在成为集“技术前沿”与“校园情报”于一体的个性化信息助理。

**当前状态**: 阶段一 & 阶段三完成（UI + 基础后端 + 自动化）✅

---

## 🎯 已完成的工作

### ✅ 核心功能实现
1.  **双主题 UI 系统**
    *   Claude 风格羊皮纸模式 (Frank Ruhl Libre 字体 + 噪点纹理)
    *   沉浸式极夜模式
2.  **数据层集成**
    *   Supabase 云端数据库连接成功
    *   Python 数据抓取脚本 (`fetch_github_trending.py`)
    *   智能降级机制 (无网/无配置自动切换模拟数据)
3.  **自动化工作流**
    *   GitHub Actions 每日定时抓取 (`.github/workflows/daily_update.yml`)
    *   覆盖领域: AI, 前端, Vercel 部署, 效率工具

### ✅ 关键文件清单
*   `app/lib/main.dart` - 主入口
*   `app/lib/features/feed/presentation/pages/feed_page.dart` - 首页 Feed
*   `app/lib/features/reader/presentation/pages/reader_page.dart` - 文章阅读页
*   `scripts/fetch_github_trending.py` - GitHub 数据抓取脚本
*   `.github/workflows/daily_update.yml` - 自动化 CI/CD

---

## 📅 下一阶段开发计划 (Phase 4 & 5)

### Phase 4.1: 校园情报局 (SCUT JW Crawler) - **[Next Priority]**
**目标**：开发针对华南理工大学教务处的爬虫，不再错过任何选课与考试通知。

*   **技术栈**: `requests`, `BeautifulSoup`, `html2text`
*   **脚本路径**: `scripts/fetch_scut_jw.py`
*   **核心逻辑**:
    1.  抓取 `https://jw.scut.edu.cn/zhinan/cms/toPosts.do`
    2.  **智能过滤**:
        *   **强关注**: `微电子`, `集成电路`, `芯片`, `保研`, `实习`, `广州国际校区`, `GZIC`
        *   **弱关注**: `选课`, `放假`, `通知` (通用类)
    3.  **格式转换**: 将教务处 HTML 通告转换为 Markdown 存入数据库。

### Phase 4.2: AI 深度解读引擎
**目标**：引入 LLM (Claude/GPT) 对 GitHub 项目和新闻进行深度摘要，生成结构化技术报告。

*   **流程**: 抓取原始内容 -> 调用 LLM API 生成摘要 -> 存入 Supabase。

### Phase 5: 体验优化
1.  **收藏功能**: 实现本地/云端文章收藏同步。
2.  **归档页面**: 展示已收藏的历史文章。
3.  **UI 区分**: 在 Feed 流中通过图标区分 "GitHub", "News", "School Notice"。

---

## 🚀 常用命令备忘

### 运行 App
```bash
# 开发模式 (自动连接 Supabase)
flutter run

# 如果遇到连接问题，可强制指定 Key (仅调试用)
# flutter run --dart-define=SUPABASE_URL=...
```

### 运行数据抓取脚本
```bash
# 抓取 GitHub 热门 (带上传)
python scripts/fetch_github_trending.py --language python --limit 10 --upload

# (未来) 抓取教务通知
python scripts/fetch_scut_jw.py --upload
```

---

## ✅ 开发检查清单

### 阶段一至三 (已完成)
- [x] UI 框架搭建 (Feed, Reader, Themes)
- [x] Supabase 数据库集成
- [x] GitHub Trending 数据自动化
- [x] GitHub Actions 配置

### 阶段四 (进行中)
- [ ] **编写华工教务处爬虫 (`scripts/fetch_scut_jw.py`)**
- [ ] 测试教务数据入库
- [ ] App 端适配教务文章样式 (日期高亮, 来源图标)

### 阶段五 (规划中)
- [ ] 接入 AI API 进行内容摘要
- [ ] 实现收藏夹功能
- [ ] 发布 Android APK
